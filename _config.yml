title: Sophuckit Podcast
description: "No-holds-barred investigative comedy. We expose corruption, hypocrisy, and evil with savage wit. Adults only."
tagline: "Exposing the Phucking Truth."
baseurl: "/sophuckit.github.io"
url: "https://princemanjee.github.io"

collections:
  episodes:
    output: true
  guests:
    output: true
  hosts:
    output: true
  seasons:
    output: true

defaults:
  - scope:
      path: ""
      type: "episodes"
    values:
      layout: "episode"
  - scope:
      path: ""
      type: "guests"
    values:
      layout: "guest"

markdown: kramdown
# Do not use any theme, we have our own layouts
theme: 
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - package-lock.json
  - .jekyll-cache
  - .sass-cache
